# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.db
.env
.sandbox/
.e2e_sandbox/
.coverage

# KROFT_OS merge additions (Variant A)
data/
*.exe
.hermes/
archive/KnowledgeOS-v5/
recovery_journal.jsonl
stubs/
.venv/
cache/
.DS_Store
*.egg-info/

# Eval artifacts (generated, not source)
tests/calibration.json
tests/embedding_eval_results.json

# Large knowledge foundation snapshot + extracted sources (NOT source code).
# The production snapshot (_snapshot.json, ~750MB) and its backups / extracted
# corpora live here locally but must NEVER be committed or pushed (GitHub 100MB
# file / 2GB repo limits). They are regenerated by the foundation pipeline.
KROFT_KNOWLEDGE_FOUNDATION/
