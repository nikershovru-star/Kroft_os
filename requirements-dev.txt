# KROFT_OS v5 — DEV / TEST dependencies
# Generated by Hermes Agent — R1 dependency audit (2026-08-15).
# Mirrors the test tooling actually used by the suite (pytest.ini: asyncio_mode=strict).
# These are NOT needed to RUN the kernel, only to TEST it.

-r requirements.txt

pytest>=8.0            # test runner (suite uses modern marks + asyncio_mode=strict)
pytest-asyncio>=0.21   # required by pytest.ini asyncio_mode=strict + async tests
pytest-cov>=4.0        # coverage (ТЗ Этапа 6 quality gate; optional but standard)

# NOTE: fastapi/uvicorn/httpx/pydantic/numpy/scipy/faiss/sentence-transformers/torch
# are INSTALLED in the dev hermes-venv but NOT imported by current code or tests
# (web/embedding stages not yet built — see DEPENDENCIES_AUDIT.md). They are
# intentionally excluded here so the manifest stays minimal + reproducible.
# When Stage 4 (fact-check) / Stage 5 (FastAPI) lands, add them explicitly.
