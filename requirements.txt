# KROFT_OS v5 — RUNTIME dependencies (minimal, reproducible)
# Generated by Hermes Agent — R1 dependency audit (2026-08-15), NOT pip freeze.
#
# Principle (ТЗ STEP 3): only packages the CODE actually imports at runtime.
# Optional / future-stage packages are listed in requirements-optional.txt.
# Heavy ML/serving stacks (torch, sentence-transformers, faiss, fastapi, uvicorn)
# are NOT imported by current code (web/embedding stages not yet built) -> excluded.
#
# Version policy (ТЗ STEP 4): compatible ranges, not blind pins. PyYAML is the
# only hard runtime must-have (config/contract parsing); range chosen for broad
# compatibility.

PyYAML>=6.0            # config + contract-spec YAML parsing (17 import sites; only runtime must-have)

# --- optional runtime extras (NOT required for base kernel/FSM/self-evolution) ---
# Uncomment only the capabilities you enable:
# networkx>=3.0        # services/graph_query_engine.py (graph reasoning) — optional
# ollama>=0.1.0        # adapters/ollama_vision.py (local vision) — optional
# requests>=2.28       # scripts/fetch_foundation.py (corpus fetch) — optional/script-only
# psutil>=5.9          # observability / process metrics — optional
# watchdog>=3.0        # file-watch plugins — optional
# pypdf>=4.0           # PDF ingestion scripts — optional/script-only
